# 构建上下文越小，docker build 的第一步就越快——它要把整个上下文打包送给 daemon。
.git
.idea
.vscode

# 凭据绝不能进镜像。镜像里本来就没有配置文件，配置全部来自环境变量。
*.env
.env

# 构建产物
/trading-agents
/out
/dist
*.exe

# 前端依赖。几百兆、上万个文件，全部要打进构建上下文送给 daemon——
# 而镜像里用的是 web-builder 阶段自己 npm ci 装的那一份，这里带进去的没人用。
web/node_modules

# 宿主机上的前端产物与中间物。
#
# web/dist 排掉不只是省上下文：`COPY --from=web-builder` 是合并不是替换，
# 宿主上次构建的旧 chunk 会存活并被 go:embed 一起编进二进制。
web/dist
web/.openapi3.json
**/*.tsbuildinfo

# air 的热加载产物，宿主架构的二进制，对 linux 镜像毫无用处。
tmp/

# 文档与杂项对运行时毫无用处
*.md
!README.md
Makefile
Dockerfile
docker-compose.yml
.dockerignore
